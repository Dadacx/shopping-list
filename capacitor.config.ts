import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dadacx.shopping_list',
  appName: 'Lista zakupów',
  webDir: 'build'
};

export default config;
